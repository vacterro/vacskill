# Phase: DONE

There is no more work to do on the current ticket.

1. If the user wants to start a new project or big feature, run `saipen goal` (sets phase to PLAN).
2. If there are unresolved bugs, run `saipen fix SYMPTOM`.
3. If the board is clear and the user simply typed `/saipen`, you MUST immediately run `saipen (hunt)` (sets phase to HUNT) to look for bugs.
4. If the user asks to add new features or brainstorm, run `saipen (add)` (sets phase to ADD).
