# Board
## DOING
## TODO
## DONE
- [x] T-148 doc_drift: all 33 guides/GUIDE_XX.md files (32 languages + bonus Дед voice) checked individually -- 29 had neither `saipen markhunt` nor `saipen prepare` (plain 8-row tables: set/continue/stop/status/goal/clean/translate/ship), 4 (EN/RU/EE/DED) already had markhunt from an earlier manual pass but still lacked prepare. Added whichever was missing to each file, matching that file's own existing style (rich 3-column persona table for EN/RU, simpler description-only for EE/DED, plain 1-line action for the other 29) and own row order, never forcing GUIDE.md's exact canonical order onto a file that had already established its own. Deliberately did NOT backfill other pre-existing gaps noticed along the way (`validate`, `plan`, `status`, `goal` missing from various subsets) -- those predate this session's own markhunt/prepare additions and are a separate, broader concern, not this ticket's scope. Both validators green.
- [x] T-147 [SUBAUDIT] design_gap: validate.py now recurses into `.saipen/extensions/subs/*/STATE.md` (or legacy root), checking each against the same state.schema.json (never a separate restricted copy) plus two subSaipen-only policy checks (`mode: read-only`, no write-requiring `phase`) -- this also happens to satisfy OP-19's second alternative fix (a validator check) without the disproportionate first alternative (restricting the shared schema) T-146 already declined. Added `extensions/schemas/outbox.schema.json` as descriptive-only reference, same footing as board/log schemas -- OUTBOX.md's freeform prose shape isn't machine-parsed today, that would be its own separate BUILD. CONFORMANCE row 29 added. Both live subSaipens (saiwiki, saihunt) verified passing.
- [x] T-146 [SUBAUDIT] Operational Safety (OP-13..26, from tofix/SAIPEN_SUBSAIPEN_AUDIT.md) -- 9 real+fixed in extensions/subs/PROTOCOL.md: OP-13 (collect write-order: ticket+LOG first, mark-reviewed last, matching RFC §1.5's crash-safety asymmetry -- worst case on crash is a safe duplicate, never a lost finding), OP-14 (added `saipen sub status`), OP-15 (added pause/resume via the already-legal BLOCKED phase), OP-17 (MANIFEST `last_collect` timestamp), OP-18 (`sub list` flags BLOCKED entries + tied review to the existing HUNT cadence), OP-20 (saipen_home verified before spawn copies from it), OP-21 (subSaipen's own LOG MAY mirror collection), OP-23 (clean now actively checks BOARD/OUTBOX and refuses, not just states the precondition), OP-25 (hunt.md's kitchen scope widened to `.saipen/extensions/subs/*/kitchen/`), OP-26 (spawn refuses if `<name>` already exists instead of silently overwriting). 2 dismissed as false alarms: OP-16 (already RFC §1.4's accepted concurrency boundary, not a subSaipen-specific gap), OP-19 (its "fix" contradicts §1's own explicit "procedural, not technical enforcement" design choice, stated in the same doc). 1 deferred to T-147: OP-24 (validate.py coverage is real code work, not a doc-sync fix).
- [x] T-145 [SUBAUDIT] ID & Traceability (ID-08..12) -- 3 real+fixed: ID-08 (defined the T-### translation rule: original subSaipen ID preserved in the new ticket's own text, never reused as the ticket ID), ID-09 (textual `<name>-### -> T-###` convention in the collect RUN: line -- RFC's `parent:` can't reach across separate LOG.md files, so this does the traceability job in plain text instead, no RFC change needed), ID-10 (TEMPLATE/STATE.md gained `saipen_home: ""`, placeholder tidied `(name)` -> `<name>`, spawn now sets both). 2 dismissed as false alarms: ID-11 (RFC §1.2 already fully specifies ticket shape; a redundant TEMPLATE example adds nothing), ID-12 (TEMPLATE/kitchen/OUTBOX.md already exists on disk -- the audit's own file snapshot was just incomplete).
- [x] T-144 [SUBAUDIT] OUTBOX Lifecycle (OUTBOX-01..07) -- 6 real+fixed: OUTBOX-01 (added `reviewed` status; pruning rides the existing HUNT/clean kitchen-staleness sweep instead of a new archive file), OUTBOX-03 (backpressure: a self-planning subSaipen self-blocks past 10 unreviewed `ready` entries), OUTBOX-04 (freshness spot-check at collect time against the whole backlog, not just per-ticket at prepare), OUTBOX-05 (optional `severity: P0|P1|P2`), OUTBOX-06 (main agent may skip/leave entries `ready`, never forced to swallow the whole OUTBOX), OUTBOX-07 (`_shared/inbox.md` given real shape, a prune rule, main-agent ownership with subSaipen append-only, and a PLAN-reads-it trigger). 1 dismissed as false alarm: OUTBOX-02 (PROTOCOL.md §4 already has the subSaipen mark its own ticket DONE at prepare-time, independent of the main agent's later disposition -- no feedback loop was ever needed).
- [x] T-140 [MARKHUNT] Checked against live RFC §2.4 -- dismissed. The dual-cap ("whichever comes first") is a deliberate safety-valve design, not a flaw: a 3-waves/0-tickets run only happens if ADD keeps failing to ship anything, which is exactly when stopping to checkpoint is correct; a genuinely mature/clean product exits earlier anyway via the existing graceful-conclusion Exit condition. Catching a runaway on either axis is the point of two orthogonal caps, not a gap in them.
- [x] T-138 [MARKHUNT] Confirmed real -- added hysteresis to verify.md's Debug cap: a ticket blocked a second time (its `| blocker:` already carries prior-round text) now escalates straight to session-level `BLOCKED` instead of spending a fresh 3-hypothesis/2-fix-cycle budget retrying the same approach. CONFORMANCE row 28 added.
- [x] T-137 [MARKHUNT] Checked against live RFC §1.2 -- dismissed. `next_action`'s single-field design is already deliberate: `WAIT: <question>` is explicitly defined as executable ("ask this exact thing and stop" is itself an action), which already unifies the human-question and command audiences the audit wanted split apart. Splitting into next_phase/next_human_gate would add two fields to keep in sync to solve something already resolved.
- [x] T-133 [MARKHUNT] 4 sub-parts checked against live files. `blocker: none` vs empty: false alarm -- RFC's non-empty rule only fires when `phase: BLOCKED`, "none" as a not-currently-blocked default is fine. Checkpoint order: false alarm -- §1.5 already fully specifies LOG->BOARD->STATE with reasoning. Phase history: already documented as an accepted conceptual-only gap (CONFORMANCE row 14; also bumped its stale "fourteen"-phase count to sixteen while there, a real drift this ticket's own re-check caught). HUNT-vs-BLOCKED duplicate tickets: real, fixed -- hunt.md now checks all of `BOARD.md` including `## BLOCKED` before ticketing a finding, so a known-but-blocked issue can't fork into two records.
- [x] T-132 [MARKHUNT] 3 sub-parts checked against live init.md/plan.md/RFC §1.10 -- all already consistent (bare `saipen goal`'s resume clause, init.md's WAIT phrasing, plan.md's size gate). No formatting defect found against current files; likely already resolved by this session's earlier §1.10 bare-goal fix. Closed as false alarm.
- [x] T-131 [MARKHUNT] Both sub-parts real, fixed. RFC §2.4 now states the `goal_waves` HUNT->ADD increment fires the moment `ADD`'s own evaluation reaches a `RETURN`, not when the resulting ticket later finishes its own BUILD/VERIFY/SHIP run. README's Explicit Triggers bullet expanded from 4 commands to include markhunt/prepare/status/stop, with a pointer to RFC §1.10 for the full list.
- [x] T-130 [MARKHUNT] Confirmed real -- RFC §1.10's unknown-command rejection never checked active project extensions' own bare-command vocabulary (§1.9) first, directly contradicting §1.9's own carve-out for e.g. `saipen sub spawn`. Fixed: the rejection rule now checks extensions before concluding a word is unrecognized.
- [x] T-129 [MARKHUNT] Confirmed real -- `saipen stop`'s interaction with `goal_mode` was never stated directly, only inferable by cross-referencing §2.4's exhaustive Exit list. RFC §1.10 now states plainly: stop pauses, never clears, `goal_mode`; any later resume continues under the same still-`true` `goal_mode`.
- [x] T-127 [MARKHUNT] 3 sub-parts checked. `VERIFY->SCOUT|BUILD`: already fixed earlier this session. `BLOCKED->DONE`: false alarm -- `blocked.md` step 5 already documents it plainly. `DONE->ADD`: real, fixed -- `done.md` itself says ADD is only ever reached via a clean HUNT, so the table's own `DONE->ADD` entry was never actually implemented by any phase doc; removed it with an explanatory line.
- [x] T-126 [MARKHUNT] Confirmed real -- RFC §1.2's LOG exact-shape line omitted the optional `agent:` slot and the `T-none` alternative that the prose right below it already documents correctly. Skeleton updated to `[T-### \| T-none] [agent: <id>]`, with a note that it's a quick-reference shape, not a second source of truth.
- [x] T-124 [MARKHUNT] Confirmed real -- GUIDE.md's markhunt/prepare command rows never propagated to the 32 hand-maintained `guides/GUIDE_XX.md` siblings, and nothing owned that sync: TRANSLATE's own drift-rescan (§3) only watches surfaces it builds itself, permanently excluding hand-maintained siblings per its own §2 carve-out. `translate.md` now states this boundary explicitly instead of leaving it assumed. Concrete 32-file sync spun into its own ticket (T-148) rather than rushed inline here.
- [x] T-121 Translate batch 6/6 (sk, hr, ded) | owner: antigravity-gemini | claim_time: 2026-07-23T08:07:00Z | verify: ls .saipen/saitranslate/kitchen/*/*.md
- [x] T-120 Translate batch 5/6 (id, el, cs, ro, hu, bg) | owner: antigravity-gemini | claim_time: 2026-07-23T08:07:00Z | verify: ls .saipen/saitranslate/kitchen/*/*.md
- [x] T-119 Translate batch 4/6 (th, vi, ar, he, tr, hi) | owner: antigravity-gemini | claim_time: 2026-07-23T08:07:00Z | verify: ls .saipen/saitranslate/kitchen/*/*.md
- [x] T-115 Wire MARKHUNT phase into RFC -- done by user's own direct edits (enum+table, §1.10, §2.1, CONFORMANCE row 27, GUIDE); this session added the one missed piece, `saipen prepare` never got its own §1.10 bullet or GUIDE row -- added.
- [x] T-122 [MARKHUNT] RFC §2.2 ADD pseudocode synced with add.md's already-fixed logic (bugfix goes TICKET+RETURN SCOUT, minimal_delta goes TICKET+CLAIM+RETURN BUILD); stale "VERIFY, then HUNT" prose replaced with the real BUILD-VERIFY-REVIEW-SHIP-DONE flow.
- [x] T-128 [MARKHUNT] add.md's minimal_delta branch now explicitly claims the ticket (TODO to DOING, owner and claim_time set) before RETURN BUILD -- folded into T-122's edit.
- [x] T-141 [MARKHUNT] Confirmed real -- tests/validate.sh and validate.ps1 both had the stale 14-phase regex; added MARKHUNT and PREPARE to both.
- [x] T-142 [MARKHUNT] Confirmed real -- extensions/schemas/state.schema.json's phase enum was the same stale 14 values; added MARKHUNT and PREPARE.
- [x] T-143 [MARKHUNT] Checked live STATE.md -- already self-resolved by the ongoing translate session's own progress (now shows task T-119 matching BOARD's current DOING entry, not the stale T-118 snapshot the finding described); no action needed.
- [x] T-123 [MARKHUNT] Confirmed real -- RFC §1.2's WAIT list had only 5 categories, blocked.md's own WAIT: usage for unblock requests wasn't among them. Added a 6th: session-level BLOCKED unblock request, question must stay concrete.
- [x] T-125 [MARKHUNT] Confirmed real -- RFC §2.1's preamble and ZERO-PROMPT line both said board.md is empty, its own HUNT bullet two lines down said no open TODO tickets without blockers. Unified both to the precise phrasing; folded T-135 (same root cause) into the same edit. README's two Board empty mentions softened to match.
- [x] T-135 [MARKHUNT] Same fix as T-125 -- preamble/body inconsistency in §2.1 was one edit, not two.
- [x] T-134 [MARKHUNT] Confirmed real, but the audit's own suggested fix (rename BLOCKED to STALLED/HALTED) was disproportionate -- a full rename ripples through every phase doc, template, fixture, and any project's own existing STATE.md files. Applied a lighter fix instead: the RFC §1.6 transition table's own intro now states explicitly that BLOCKED there is always session-level STATE.phase: BLOCKED, distinct from board-level ## BLOCKED, right where an agent would first read the ambiguous bare word.
- [x] T-139 [MARKHUNT] Confirmed real -- a genuine self-contradiction in this session's own markhunt.md: it claimed completion "always halts one turn for the user" but transitioning to DONE with goal_mode: true would let done.md's own existing step 2 auto-proceed straight to HUNT regardless. Fixed at the root: done.md's Goal-Mode-Empty-Board step now has an explicit exception -- any [MARKHUNT]-tagged ticket sitting in ## BLOCKED blocks the auto-HUNT cascade until triaged out. markhunt.md's own text now points at this real mechanism instead of just asserting the halt happens.
- [x] T-118 Translate batch 3/6 (sv, da, fi, no, zh, ko) | owner: antigravity-gemini | claim_time: 2026-07-23T07:57:00Z | verify: ls .saipen/saitranslate/kitchen/*/*.md
- [x] T-117 Translate batch 2/6 (fr, es, it, pt, nl, pl) | owner: antigravity-gemini | claim_time: 2026-07-23T07:28:00Z | verify: ls .saipen/saitranslate/kitchen/*/*.md
- [x] T-116 Translate batch 1/6 (ru, ee, ja, uk, de) | owner: antigravity-gemini | claim_time: 2026-07-23T07:23:00Z | verify: ls .saipen/saitranslate/kitchen/*/*.md
## BLOCKED
- [ ] T-136 [MARKHUNT][P0] design_gap: MARKHUNT lacks verifiable thoroughness self-test | blocker: needs real design, not a quick doc-sync fix -- deliberately deferred; HUNT has a hash-match rule as a hard skip check, MARKHUNT has nothing analogous, progress file is self-reported with no external cross-verification | verify: Design a manifest-driven closure check before implementing -- what exactly must be recorded, what would VALIDATE cross-check it against, without building busywork ceremony
