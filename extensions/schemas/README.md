# schemas/ — frozen until an orchestrator exists

JSON Schemas for STATE/BOARD/LOG. No agent reads these; they wait for a
future external validator/orchestrator that parses `.vacskill/`
programmatically. Do not extend them speculatively — the Markdown files in
`templates/` are the living spec.
