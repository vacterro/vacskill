# schemas/ РІР‚вЂќ frozen until an orchestrator exists

JSON Schemas for STATE/BOARD/LOG. No agent reads these; they wait for a
future external validator/orchestrator that parses `.saipen/`
programmatically. Do not extend them speculatively РІР‚вЂќ the Markdown files in
`templates/` are the living spec.
