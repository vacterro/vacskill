# Qwen adapter (Qwen Agent / Open WebUI)

- No speculative prose before commands; execute, then report.
- Code blocks must carry exact target filenames — local runners depend on it.

Everything else: follow `saipen/RFC.md`.
